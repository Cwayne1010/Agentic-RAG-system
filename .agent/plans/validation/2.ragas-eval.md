# Validation — Part 2: RAGAS Evaluation

Automated answer-quality scoring. Requires: docs uploaded + `pip install ragas`.

---

### R1 — Golden set generation succeeds
```bash
cd backend && source venv/bin/activate
python -m eval.generate_golden_set \
    --user-id 6fff7599-6679-4d80-8f3b-60d5353f5028 \
    --output eval/golden_set.json
```
**Pass:** `golden_set.json` created with 10 entries, each containing `question` and `ground_truth` fields
**Fail:** Script errors, or output file is empty

### R2 — Evaluation run produces scores
```bash
python -m eval.run_eval \
    --user-id 6fff7599-6679-4d80-8f3b-60d5353f5028 \
    --golden-set eval/golden_set.json \
    --output eval/results.json \
    --metrics faithfulness context_precision
```
**Pass:** `results.json` written with scores between 0 and 1; printed to terminal
**Fail:** Script errors, or scores are null/NaN

### R3 — Faithfulness > 0.5 on a factual document
Use `Tickler_System_Executive_Summary.md` (already uploaded and used in R2).

**Pass:** `faithfulness` score > 0.5 — LLM is grounding answers in retrieved context
**Fail:** Score ≤ 0.5 — LLM is ignoring context or hallucinating

> **Last result:** faithfulness = **0.936** ✅

---

## Pass/Fail Tracker

```
[x] R1  Golden set generation      (10 varied pairs)
[x] R2  Eval run produces scores   (--metrics faithfulness)
[x] R3  Faithfulness > 0.5         (score: 0.936)
```
