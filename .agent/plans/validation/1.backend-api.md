# Validation — Part 1: Supabase & Backend

API (curl) + SQL editor tests. No browser required.

---

## Pre-Flight

### P1 — Backend running
```bash
curl -s http://localhost:8000/health
```
**Pass:** `{"status":"ok"}`
**Fail:** Connection refused or any other response

### P3 — Environment variables set
```bash
cd backend && source venv/bin/activate && python -c "
import os
required = ['SUPABASE_URL','SUPABASE_SERVICE_ROLE_KEY','OPENROUTER_API_KEY','OPENAI_API_KEY','LANGSMITH_API_KEY']
missing = [k for k in required if not os.getenv(k)]
print('MISSING:', missing if missing else 'none')
"
```
**Pass:** `MISSING: none`
**Fail:** Any variable listed

---

## Getting the Auth Token (for curl tests)

After logging into `http://localhost:5173`, open DevTools console and run:
```javascript
const s = await (await import('/src/lib/supabaseClient.js')).supabase.auth.getSession();
console.log(s.data.session.access_token);
```
Then in your terminal: `export TOKEN=<paste_token_here>`

---

## Auth API

### A3 — JWT validation rejects bad tokens
```bash
curl -s -w "\nHTTP %{http_code}" http://localhost:8000/api/conversations \
  -H "Authorization: Bearer fake_token_12345"
```
**Pass:** `HTTP 401` with error message
**Fail:** `HTTP 200` or any data returned

---

## Conversations API

### C1 — Create a conversation
```bash
curl -s -w "\nHTTP %{http_code}" -X POST http://localhost:8000/api/conversations \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title": "Test Conversation"}'
```
**Pass:** `HTTP 201` with `{id, title, created_at, updated_at}`; save the `id` as `$CONV_ID`
**Fail:** Non-201 or missing fields

### C2 — List conversations
```bash
curl -s -w "\nHTTP %{http_code}" http://localhost:8000/api/conversations \
  -H "Authorization: Bearer $TOKEN"
```
**Pass:** `HTTP 200` with JSON array containing the conversation from C1
**Fail:** Non-200 or empty when conversations exist

### C3 — Update conversation title
```bash
curl -s -w "\nHTTP %{http_code}" -X PATCH http://localhost:8000/api/conversations/$CONV_ID \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title": "Renamed Conversation"}'
```
**Pass:** `HTTP 200` with updated title
**Fail:** Non-200 or title unchanged

### C4 — Delete conversation
```bash
curl -s -w "\nHTTP %{http_code}" -X DELETE http://localhost:8000/api/conversations/$CONV_ID \
  -H "Authorization: Bearer $TOKEN"
```
**Pass:** `HTTP 200` or `204`; conversation gone from subsequent GET
**Fail:** Non-200/204 or conversation still in list

### C5 — Messages cascade-delete with conversation (SQL)
```sql
SELECT COUNT(*) FROM messages WHERE conversation_id = '<deleted_conv_id>';
```
**Pass:** Count = 0
**Fail:** Any orphaned messages remain

---

## Chat & SSE

### CH4 — Full chat history stored correctly (SQL)
```sql
SELECT role, LEFT(content, 80) as preview, created_at
FROM messages
WHERE conversation_id = '<your_conv_id>'
ORDER BY created_at;
```
**Pass:** Both `user` and `assistant` rows in order with correct roles
**Fail:** Missing messages, wrong roles, or only latest turn stored

### CH5 — SSE endpoint returns correct event format
```bash
curl -s -N -X POST "http://localhost:8000/api/conversations/$CONV_ID/messages" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "Say hello"}' 2>&1 | head -20
```
**Pass:** Lines prefixed with `data:` visible; `delta` events followed by `done` event at end
**Fail:** Plain JSON response or empty body

---

## Documents API + SQL

### D2 — List documents API
```bash
curl -s -w "\nHTTP %{http_code}" http://localhost:8000/api/documents \
  -H "Authorization: Bearer $TOKEN"
```
**Pass:** `HTTP 200` with array containing `test.txt` with `status: "completed"` and `chunk_count > 0`
**Fail:** Empty array when documents exist, or stale status

### D3 — Chunks stored with 1536-dim embeddings (SQL)
```sql
SELECT d.filename,
       COUNT(dc.id) AS chunk_count,
       vector_dims(dc.embedding) AS embedding_dims
FROM documents d
JOIN document_chunks dc ON dc.document_id = d.id
WHERE d.filename = 'test.txt'
GROUP BY d.filename, vector_dims(dc.embedding);
```
**Pass:** Row with `chunk_count >= 1` and `embedding_dims = 1536`
**Fail:** No rows, or embedding_dims ≠ 1536

### D4 — Delete document cascades chunks (SQL)
```sql
SELECT COUNT(*) FROM document_chunks
WHERE document_id NOT IN (SELECT id FROM documents);
```
**Pass:** 0 orphaned chunks
**Fail:** Orphaned chunks remain

---

## Vector Search & RAG

### V1 — Vector search RPC function works (SQL)
```sql
SELECT id, chunk_index, LEFT(content, 60) as content, similarity
FROM match_document_chunks(
  query_embedding := (SELECT embedding FROM document_chunks LIMIT 1),
  match_threshold := 0.1,
  match_count := 5,
  p_user_id := (SELECT id FROM auth.users WHERE email = 'flocker@login.com')
);
```
**Pass:** At least one row returned with `similarity` column ≥ 0.1
**Fail:** Function not found, or zero rows at threshold 0.1

### V3 — Retrieval is user-scoped (SQL)
```sql
SELECT COUNT(*) FROM document_chunks;
```
Cross-verify: count should match only flocker's uploaded documents.

**Pass:** Count equals only chunks for documents owned by flocker
**Fail:** Count includes other users' data (RLS misconfigured)

### V4 — Retrieval SSE event arrives before first delta (curl)
```bash
curl -s -N -X POST "http://localhost:8000/api/conversations/$CONV_ID/messages" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "How many hours per month does the manual tickler process consume?"}' 2>&1 | head -5
```
**Pass:** First `data:` line has `"type":"retrieval"` before any `"type":"delta"` line
**Fail:** First line is `delta`

---

## Settings API

### S1 — Get current settings
```bash
curl -s -w "\nHTTP %{http_code}" http://localhost:8000/api/settings \
  -H "Authorization: Bearer $TOKEN"
```
**Pass:** `HTTP 200` with `{embedding_model, chat_model}` in response
**Fail:** Non-200 or missing fields

### S2 — Update chat model
```bash
curl -s -w "\nHTTP %{http_code}" -X PATCH http://localhost:8000/api/settings \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"chat_model": "google/gemini-flash-1.5"}'
```
**Pass:** `HTTP 200` with updated `chat_model`
**Fail:** Non-200 or model unchanged

### S3 — Validate model rejects invalid model ID
```bash
curl -s -w "\nHTTP %{http_code}" -X POST http://localhost:8000/api/settings/validate-model \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"model_id": "fake/nonexistent-model-xyz-abc"}'
```
**Pass:** Non-200 response (4xx) or `{valid: false}`
**Fail:** `HTTP 200` confirming an invalid model

### S4 — Embedding model locked when documents exist
```bash
curl -s -w "\nHTTP %{http_code}" -X PATCH http://localhost:8000/api/settings \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"embedding_model": "text-embedding-3-large"}'
```
**Pass:** Request rejected (4xx) with a message explaining documents exist
**Fail:** Embedding model changed while documents are present

---

## Data Isolation (RLS)

### I1 — Flocker cannot see Clocker's conversations via API
```bash
curl -s http://localhost:8000/api/conversations -H "Authorization: Bearer $FLOCKER_TOKEN"
```
**Pass:** Clocker's conversation not in the response
**Fail:** Both users' conversations returned

### I2 — Flocker cannot see Clocker's documents via API
```bash
curl -s http://localhost:8000/api/documents -H "Authorization: Bearer $FLOCKER_TOKEN"
```
**Pass:** Only flocker's documents returned
**Fail:** Clocker's documents appear in the list

### I3 — RLS policies exist on all user tables (SQL)
```sql
SELECT tablename, policyname, cmd, roles
FROM pg_policies
WHERE tablename IN ('conversations', 'messages', 'documents', 'document_chunks')
ORDER BY tablename, policyname;
```
**Pass:** At least one RLS policy per table visible
**Fail:** Any table has zero policies

---

## SQL Template Architecture

### SQL1 — All 8 RPC functions exist (SQL)
```sql
SELECT routine_name
FROM information_schema.routines
WHERE routine_schema = 'public'
  AND routine_name LIKE 'rpc_%'
ORDER BY routine_name;
```
**Pass:** 8 rows: `rpc_count_chunks_by_document`, `rpc_count_documents`, `rpc_count_messages_in_conversation`, `rpc_find_documents_by_status`, `rpc_get_recent_conversations`, `rpc_list_documents`, `rpc_search_chunks_by_keyword`, `rpc_storage_summary`

### SQL2 — Valid template executes and returns data
```bash
cd backend && source venv/bin/activate && python -c "
import asyncio, os
from dotenv import load_dotenv; load_dotenv(override=True)
from app.tools.sql_templates import execute_query

async def test():
    r = await execute_query('count_documents', {}, '6fff7599-6679-4d80-8f3b-60d5353f5028')
    print('count_documents result:', r.rows)

asyncio.run(test())
"
```
**Pass:** Prints row with `document_count` field

### SQL3 — Invalid template name is rejected
```bash
cd backend && source venv/bin/activate && python -c "
import asyncio, os
from dotenv import load_dotenv; load_dotenv(override=True)
from app.tools.sql_templates import execute_query

async def test():
    try:
        await execute_query('DROP TABLE documents', {}, 'any-id')
        print('FAIL: should have raised ValueError')
    except ValueError as e:
        print('Pass — rejected:', e)

asyncio.run(test())
"
```
**Pass:** Prints `Pass — rejected: Unknown template...`

### SQL4 — Missing required param is rejected
```bash
cd backend && source venv/bin/activate && python -c "
import asyncio, os
from dotenv import load_dotenv; load_dotenv(override=True)
from app.tools.sql_templates import execute_query

async def test():
    try:
        await execute_query('find_documents_by_status', {}, '6fff7599-6679-4d80-8f3b-60d5353f5028')
        print('FAIL: should have raised ValueError')
    except ValueError as e:
        print('Pass — rejected:', e)

asyncio.run(test())
"
```
**Pass:** Prints `Pass — rejected: Template 'find_documents_by_status' requires param 'status'...`

---

## Module 3: Record Manager

### RM1 — content_hash column exists (SQL)
```sql
SELECT column_name FROM information_schema.columns
WHERE table_name = 'documents' AND column_name = 'content_hash';
```
**Pass:** 1 row returned
**Fail:** 0 rows — run migration 008_content_hash.sql

### RM2 — Partial unique index exists (SQL)
```sql
SELECT indexname, indexdef FROM pg_indexes
WHERE tablename = 'documents' AND indexname = 'uq_documents_user_completed_hash';
```
**Pass:** 1 row; `indexdef` contains `WHERE (status = 'completed')`
**Fail:** No rows

### RM3 — Duplicate upload rejected with 409 (curl)
```bash
printf "hello dedup world" > /tmp/test_dedup.txt
# First upload — should succeed:
curl -s -w "\nHTTP %{http_code}" -X POST http://localhost:8000/api/documents/upload \
  -H "Authorization: Bearer $TOKEN" -F "file=@/tmp/test_dedup.txt"
# Wait for status=completed in DB or UI, then upload again:
curl -s -w "\nHTTP %{http_code}" -X POST http://localhost:8000/api/documents/upload \
  -H "Authorization: Bearer $TOKEN" -F "file=@/tmp/test_dedup.txt"
```
**Pass:** Second upload returns `HTTP 409`; body contains `"Duplicate"` and the original filename
**Fail:** Second upload returns 200 and creates a second document record

### RM4 — Incremental update: same filename, new content (curl)
```bash
printf "version one content" > /tmp/update_test.txt
curl -s -X POST http://localhost:8000/api/documents/upload \
  -H "Authorization: Bearer $TOKEN" -F "file=@/tmp/update_test.txt"
# Wait for completed, note the returned id as OLD_ID. Then:
printf "version two - completely different content" > /tmp/update_test.txt
curl -s -w "\nHTTP %{http_code}" -X POST http://localhost:8000/api/documents/upload \
  -H "Authorization: Bearer $TOKEN" -F "file=@/tmp/update_test.txt"
```
**Pass:** Second upload returns `HTTP 200` with a new `id`; verify only one row in DB:
```sql
SELECT id, filename, status FROM documents WHERE filename = 'update_test.txt';
```
Pass: 1 row only. Fail: 2 rows with same filename.

### RM5 — Re-upload of failed document is allowed (manual)
Manually set a document to failed, then re-upload the same file:
```sql
-- Replace <doc_id> with an actual document id:
UPDATE documents SET status = 'failed', error_message = 'test failure' WHERE id = '<doc_id>';
```
Then upload the same file via the UI or curl.

**Pass:** Upload returns `HTTP 200`; new ingestion begins
**Fail:** `HTTP 409` — failed docs should never block re-upload

### RM6 — Cross-user: same content uploadable by both users (curl)
Using Clocker's token, upload a file that Flocker already has as completed:
```bash
curl -s -w "\nHTTP %{http_code}" -X POST http://localhost:8000/api/documents/upload \
  -H "Authorization: Bearer $CLOCKER_TOKEN" -F "file=@/tmp/test_dedup.txt"
```
**Pass:** `HTTP 200` — no cross-user dedup, RLS works correctly
**Fail:** `HTTP 409` — would violate user isolation

### RM7 — content_hash stored correctly (SQL + Python)
After uploading `test_dedup.txt` from RM3:
```sql
SELECT filename, content_hash, length(content_hash) AS len
FROM documents WHERE filename = 'test_dedup.txt' LIMIT 1;
```
Compute expected hash independently:
```python
import hashlib
print(hashlib.sha256(open('/tmp/test_dedup.txt','rb').read()).hexdigest())
```
**Pass:** `len = 64`; DB value matches Python output exactly
**Fail:** Length ≠ 64, or hash mismatch

---

---

## Module 4: Metadata Extraction

### M4-1 — metadata column exists in documents table
```sql
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name = 'documents' AND column_name = 'metadata';
```
**Pass:** Row returned with `data_type = 'jsonb'`
**Fail:** No row returned

### M4-2 — match_document_chunks RPC returns doc_filename and doc_metadata
```sql
-- After uploading at least one document, run the RPC directly:
SELECT doc_filename, doc_metadata
FROM match_document_chunks(
  (SELECT embedding FROM document_chunks LIMIT 1),
  (SELECT user_id FROM document_chunks LIMIT 1),
  1
);
```
**Pass:** Row returned with non-null `doc_filename` string and `doc_metadata` JSONB (may be null if doc not yet re-indexed)
**Fail:** Column does not exist or error thrown

### M4-3 — metadata populated after ingestion
Upload a document, wait for status = completed, then:
```sql
SELECT filename, metadata->>'doc_type' AS doc_type, metadata->'topics' AS topics
FROM documents
WHERE status = 'completed'
ORDER BY created_at DESC
LIMIT 1;
```
**Pass:** `doc_type` is a non-null string (e.g. `"article"`, `"report"`); `topics` is a JSON array
**Fail:** `metadata` column is null after ingestion completes

### M4-4 — GET /documents returns metadata field
```bash
curl -s http://localhost:8000/api/documents \
  -H "Authorization: Bearer $TOKEN" | python3 -m json.tool | grep -A10 '"metadata"'
```
**Pass:** `"metadata"` key present in response with nested fields (`doc_type`, `topics`, etc.)
**Fail:** `"metadata"` key absent or null for completed documents

### M4-5 — filter_doc_type RPC parameter works
```sql
-- Assuming a completed document with doc_type = 'report' exists
SELECT count(*) FROM match_document_chunks(
  (SELECT embedding FROM document_chunks LIMIT 1),
  (SELECT user_id FROM document_chunks LIMIT 1),
  10,
  'nonexistent_type_xyz'
);
```
**Pass:** Returns 0 rows (filter excludes all chunks)
**Fail:** Returns rows (filter not applied)

---

## Module 5: Multi-Format Support

### M5-1 — PDF upload accepted and indexed
```bash
curl -s -w "\nHTTP %{http_code}" http://localhost:8001/api/documents/upload \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@/path/to/test.pdf"
```
**Pass:** HTTP 200; JSON includes `"status": "pending"`; document reaches `completed` with `chunk_count > 0`
**Fail:** HTTP 400 "Only .txt and .md" or 422 from parser

### M5-2 — DOCX upload accepted and indexed
```bash
curl -s -w "\nHTTP %{http_code}" http://localhost:8001/api/documents/upload \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@/path/to/test.docx"
```
**Pass:** HTTP 200; document reaches `completed`
**Fail:** HTTP 400 or 422

### M5-3 — HTML upload accepted and indexed
```bash
echo "<html><body><h1>Test</h1><p>Hello world.</p></body></html>" > /tmp/test.html
curl -s -w "\nHTTP %{http_code}" http://localhost:8001/api/documents/upload \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@/tmp/test.html;type=text/html"
```
**Pass:** HTTP 200; document reaches `completed`
**Fail:** HTTP 400 or 422

### M5-4 — Cascade delete removes chunks (SQL)
```sql
-- Note the document_id of any completed document
DELETE FROM documents WHERE id = '<document_id>';
SELECT count(*) FROM document_chunks WHERE document_id = '<document_id>';
```
**Pass:** count = 0
**Fail:** count > 0 (FK cascade not working)

### M5-5 — Unsupported file type rejected
```bash
curl -s -w "\nHTTP %{http_code}" http://localhost:8001/api/documents/upload \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@/path/to/image.jpg;type=image/jpeg"
```
**Pass:** HTTP 400 with "Unsupported file type" message
**Fail:** HTTP 200 or 422 from parser crash

### M6-1 — Migration 013 applied successfully (SQL)
```sql
-- Check that full-text search index exists
SELECT indexname, tablename FROM pg_indexes
WHERE indexname = 'idx_document_chunks_content_fts';
```
**Pass:** Returns one row with the index name
**Fail:** No rows returned

### M6-2 — BM25 search RPC function exists
```bash
cd backend && source venv/bin/activate && python -c "
import os
from supabase import create_client
client = create_client(os.getenv('SUPABASE_URL'), os.getenv('SUPABASE_SERVICE_ROLE_KEY'))
result = client.rpc('bm25_search_chunks', {'query_text': 'test', 'match_count': 1}).execute()
print('SUCCESS' if result.data is not None else 'FAIL')
"
```
**Pass:** `SUCCESS`
**Fail:** `FAIL` or Python exception

### M6-3 — Hybrid search returns diverse results
```bash
cd backend && source venv/bin/activate && python -c "
import asyncio
from app.services.hybrid_search_service import hybrid_search

async def test():
    results = await hybrid_search('artificial intelligence', top_k=5)
    print(f'Results: {len(results)}')
    # Check for RRF scoring metadata
    if results and 'rrf_score' in results[0]:
        print('RRF metadata: OK')
    else:
        print('RRF metadata: MISSING')

asyncio.run(test())
"
```
**Pass:** Returns 1-5 results with RRF metadata
**Fail:** Exception or missing RRF metadata

### M6-4 — Vector vs BM25 vs Hybrid comparison
```bash
cd backend && source venv/bin/activate && python -c "
import asyncio
from app.services.hybrid_search_service import hybrid_search, vector_only_search, bm25_only_search

async def test():
    query = 'machine learning'
    vector_results = await vector_only_search(query, 3)
    bm25_results = await bm25_only_search(query, 3)
    hybrid_results = await hybrid_search(query, 3)

    print(f'Vector: {len(vector_results)}, BM25: {len(bm25_results)}, Hybrid: {len(hybrid_results)}')

    # Check that hybrid includes results from both methods
    if hybrid_results:
        found_in = [r.get('found_in', []) for r in hybrid_results]
        has_both = any('bm25' in f and 'vector' in f for f in found_in)
        has_either = any(f for f in found_in)
        print(f'Fusion working: {has_both or has_either}')

asyncio.run(test())
"
```
**Pass:** All three methods return results; fusion metadata present
**Fail:** Any method fails or fusion metadata missing

### M6-5 — Search mode parameter works
```bash
cd backend && source venv/bin/activate && python -c "
import asyncio
from app.services.retrieval_service import search

async def test():
    query = 'document processing'
    hybrid = await search(query, top_k=3, search_mode='hybrid')
    vector = await search(query, top_k=3, search_mode='vector')

    print(f'Hybrid: {len(hybrid)}, Vector: {len(vector)}')

    # Check for different search provenance
    hybrid_types = [r.get('search_type') for r in hybrid if r.get('search_type')]
    vector_types = [r.get('search_type') for r in vector if r.get('search_type')]
    print(f'Types - Hybrid: {set(hybrid_types)}, Vector: {set(vector_types)}')

asyncio.run(test())
"
```
**Pass:** Both modes return results; different search provenance
**Fail:** Modes return identical results or error

---

## Execution Order

| Order | Tests | Notes |
|-------|-------|-------|
| 1 | P1, P3 | Start backend first |
| 2 | A3, C1–C5, CH4, CH5 | Get TOKEN first; save CONV_ID from C1 |
| 3 | D2, D3, D4 SQL | Needs docs uploaded (do D1 in Part 3 first) |
| 4 | V1, V3, V4 | Needs docs in DB |
| 5 | S1–S4, I1–I3 | S4 needs docs; I1/I2 need two users |
| 6 | SQL1–SQL4 | Needs 004 migration applied |
| 7 | RM1–RM7 | Needs 008 migration applied; RM6 needs CLOCKER_TOKEN |
| 8 | M4-1–M4-5 | Needs 009 migration applied; M4-3/M4-4 need a completed document |
| 9 | M5-1–M5-5 | Needs backend running with docling installed |
| 10 | M6-1–M6-5 | Needs 013 migration applied; M6-2+ need indexed documents |

---

## Pass/Fail Tracker

```
[x] P1   Backend health
[x] P3   Env vars set
[x] A3   Bad JWT rejected
[x] C1   Create conversation
[x] C2   List conversations
[x] C3   Update title
[x] C4   Delete conversation
[x] C5   Cascade delete messages
[x] CH4  History in DB
[x] CH5  SSE format
[x] D2   List documents API
[x] D3   Chunks in DB
[x] D4   Delete cascade SQL
[x] V1   RPC function works
[-] V3   User-scoped retrieval      (skip — clocker also has docs)
[x] V4   Retrieval SSE before delta
[x] S1   Get settings
[x] S2   Update chat model
[x] S3   Invalid model rejected
[x] S4   Embedding lock enforced
[x] I1   Cross-user conversations
[x] I2   Cross-user documents
[x] I3   RLS policies exist
[x] SQL1 All 8 RPCs exist
[x] SQL2 Valid template works
[x] SQL3 Invalid name rejected
[x] SQL4 Missing param rejected
[x] RM1  content_hash column exists
[x] RM2  Partial unique index exists
[x] RM3  Duplicate rejected with 409
[x] RM4  Incremental update works
[x] RM5  Failed doc re-upload allowed
[x] RM6  Cross-user isolation preserved
[x] RM7  Hash stored correctly
[ ] M4-1 metadata column exists
[ ] M4-2 RPC returns doc_filename + doc_metadata
[ ] M4-3 metadata populated after ingestion
[ ] M4-4 GET /documents returns metadata field
[ ] M4-5 filter_doc_type RPC parameter works
[x] M5-1 PDF upload and indexed
[ ] M5-2 DOCX upload and indexed
[ ] M5-3 HTML upload and indexed
[ ] M5-4 Cascade delete removes chunks
[ ] M5-5 Unsupported type rejected
[ ] M6-1 Migration 013 applied
[ ] M6-2 BM25 search RPC exists
[ ] M6-3 Hybrid search with RRF
[ ] M6-4 Vector vs BM25 vs Hybrid comparison
[ ] M6-5 Search mode parameter works
```
